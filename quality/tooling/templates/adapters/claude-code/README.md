# Claude Code / Agent Plugin adapter

This directory is a Claude-format agent plugin and can also be recognized by hosts that support Claude-format plugins. It bundles the portable AIFENCE Skill and points MCP at the installed `aifence` CLI. Install Runtime dependencies first (`npm install` in Runtime) and ensure `aifence` is on PATH, or use the project installer to write a direct stdio configuration.
