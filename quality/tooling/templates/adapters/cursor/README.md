# Cursor adapter

Merge `mcp.json` into `.cursor/mcp.json`, or run `aifence install cursor --project .`. Cursor receives AIFENCE primarily through MCP; do not duplicate the 1,300-control corpus into Cursor rules.
