# AIFENCE Runtime

Use the bundled `aifence` Agent Skill for substantial production artifact creation and modification. Prefer the AIFENCE MCP tools to classify/route/retrieve rather than loading the core repository broadly. Keep Runtime/Core truth and production-intent boundaries active.
