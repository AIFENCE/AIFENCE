import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';import {RUNTIME_ROOT} from '../src/paths.js';const p=path.join(RUNTIME_ROOT,'skill','aifence','SKILL.md');const t=fs.readFileSync(p,'utf8');test('skill frontmatter and progressive size',()=>{assert.ok(t.startsWith('---\nname: aifence\n'));assert.match(t,/description:/);assert.ok(t.split(/\r?\n/).length<500);assert.ok(t.length<12000)});test('skill keeps core lazy',()=>{assert.match(t,/Do not preload the repository/);assert.match(t,/aifence_quality_plan/)});
