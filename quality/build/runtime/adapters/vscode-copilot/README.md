# VS Code / GitHub Copilot adapter

Use the canonical skill in `.agents/skills/aifence/` and merge `mcp.json` into `.vscode/mcp.json`. The Runtime CLI can perform both changes with `aifence install vscode --project .`. The Claude-format plugin under `adapters/claude-code/` is also intentionally compatible with hosts that auto-detect that plugin format.
