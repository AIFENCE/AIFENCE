# VS Code / GitHub Copilot adapter

Use the canonical skill in `.agents/skills/biziq/` and merge `mcp.json` into `.vscode/mcp.json`. The Runtime CLI can perform both changes with `biziq install vscode --project .`. The Claude-format plugin under `adapters/claude-code/` is also intentionally compatible with hosts that auto-detect that plugin format.
