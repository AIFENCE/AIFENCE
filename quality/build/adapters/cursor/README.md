# Cursor adapter

Merge `mcp.json` into `.cursor/mcp.json`, or run `biziq install cursor --project .`. Cursor receives BizIQ primarily through MCP; do not duplicate the 1,300-control corpus into Cursor rules.
