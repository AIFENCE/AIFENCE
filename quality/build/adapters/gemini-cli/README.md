# Gemini CLI adapter

This is a Gemini CLI extension manifest with a bundled Agent Skill and BizIQ MCP server declaration. It expects the `biziq` CLI on PATH. The Runtime CLI can also install project-scoped `.agents/skills/biziq` and `.gemini/settings.json` directly.
