# Gemini CLI adapter

This is a Gemini CLI extension manifest with a bundled Agent Skill and AIFENCE MCP server declaration. It expects the `aifence` CLI on PATH. The Runtime CLI can also install project-scoped `.agents/skills/aifence` and `.gemini/settings.json` directly.
